// 조건문 - if문

// 지시사항
//  1. 숫자 20을 데이터로 갖는 변수 a를 선언합니다.
//  2. 숫자 40을 데이터로 갖는 변수 b를 선언합니다.
//  3. 조건문 형식 if ( ) { }를 입력합니다.
//  4. (조건)으로 a<b를 입력합니다.
//  5. {수행할 명령}으로 document.write("a < b");를 입력합니다.

var a = 20;
var b = 40;

if ( a < b ) {
    document.write("a < b");
}